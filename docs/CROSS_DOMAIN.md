# Кросс-доменное взаимодействие для Lumia AI

## 🌐 Обзор

Система кросс-доменного взаимодействия позволяет пользователям аутентифицироваться на `auth.lumiaai.ru` и автоматически перенаправляться на `chat.lumiaai.ru` с сохранением данных входа.

## 🏗️ Архитектура

### Домены
- **auth.lumiaai.ru** - Домен аутентификации
- **chat.lumiaai.ru** - Домен чата

### Поток взаимодействия
1. Пользователь заходит на `auth.lumiaai.ru`
2. Вводит данные для входа
3. При успешной аутентификации данные сохраняются в куки с доменом `.lumiaai.ru`
4. Пользователь автоматически перенаправляется на `chat.lumiaai.ru`
5. На домене чата данные автоматически подставляются из куки

## 🔧 Техническая реализация

### Куки с общим доменом
```typescript
// Куки устанавливаются с доменом .lumiaai.ru
// Это позволяет использовать их на всех поддоменах
document.cookie = "name=value;domain=.lumiaai.ru;path=/";
```

### Флаги безопасности
- **SameSite=lax** - Позволяет кросс-доменные запросы
- **Secure** - Только для HTTPS
- **Domain=.lumiaai.ru** - Общий домен для всех поддоменов

### Структура данных
```typescript
interface UserData {
  nickname: string;
  email?: string;
  subscription?: string;
  // Другие данные пользователя
}
```

## 📁 Файлы системы

### Основные утилиты
- `lib/utils/cookies.ts` - Работа с куки
- `lib/utils/cross-domain.ts` - Кросс-доменное взаимодействие
- `components/auth-guard.tsx` - Защита роутов на домене чата

### Обновленные компоненты
- `app/(auth)/login/page.tsx` - Страница входа с перенаправлением
- `components/sign-out-form.tsx` - Выход с очисткой куки

## 🚀 Использование

### На домене аутентификации (auth.lumiaai.ru)

```typescript
import { handleSuccessfulAuth } from '@/lib/utils/cross-domain';

// После успешной аутентификации
const userData = {
  nickname: 'username',
  email: 'user@example.com',
  subscription: 'premium'
};

handleSuccessfulAuth(userData, rememberMe);
// Автоматически перенаправляет на chat.lumiaai.ru
```

### На домене чата (chat.lumiaai.ru)

```typescript
import { AuthGuard } from '@/components/auth-guard';
import { useAuth } from '@/components/auth-guard';

// Защита роута
function ChatPage() {
  return (
    <AuthGuard>
      <ChatComponent />
    </AuthGuard>
  );
}

// Получение данных пользователя
function ChatComponent() {
  const { user, isLoading } = useAuth();
  
  if (isLoading) return <div>Loading...</div>;
  
  return (
    <div>
      <h1>Welcome, {user.nickname}!</h1>
      {/* Остальной контент */}
    </div>
  );
}
```

## 🔄 Поток аутентификации

### 1. Вход на auth.lumiaai.ru
```
Пользователь → auth.lumiaai.ru/login
↓
Ввод данных
↓
Проверка аутентификации
↓
Сохранение в куки (.lumiaai.ru)
↓
Перенаправление на chat.lumiaai.ru
```

### 2. Доступ к chat.lumiaai.ru
```
Пользователь → chat.lumiaai.ru
↓
AuthGuard проверяет куки
↓
Если аутентифицирован → показываем чат
↓
Если не аутентифицирован → перенаправление на auth.lumiaai.ru
```

### 3. Выход
```
Пользователь → Выход
↓
Очистка куки
↓
Перенаправление на auth.lumiaai.ru
```

## 🛡️ Безопасность

### Защита от CSRF
- SameSite=lax предотвращает CSRF атаки
- Токены аутентификации в куки

### Валидация данных
- Проверка токенов на сервере
- Валидация данных пользователя

### Ограничения
- Куки доступны только на поддоменах lumiaai.ru
- Автоматическая очистка при выходе

## 🧪 Тестирование

### Локальная разработка
```bash
# Настройка hosts файла
127.0.0.1 auth.lumiaai.local
127.0.0.1 chat.lumiaai.local

# Запуск серверов
npm run dev -- --port 3000 # auth.lumiaai.local:3000
npm run dev -- --port 3001 # chat.lumiaai.local:3001
```

### Проверка куки
```javascript
// В консоли браузера
console.log(document.cookie);
// Должны быть видны куки с доменом .lumiaai.ru
```

## 🔧 Настройка

### 1. DNS настройки
```
auth.lumiaai.ru → Сервер аутентификации
chat.lumiaai.ru → Сервер чата
```

### 2. SSL сертификаты
- Wildcard сертификат для *.lumiaai.ru
- Или отдельные сертификаты для каждого поддомена

### 3. CORS настройки
```typescript
// На сервере аутентификации
app.use(cors({
  origin: ['https://chat.lumiaai.ru'],
  credentials: true
}));
```

## 🐛 Отладка

### Проблемы с куки
1. Проверьте домен куки в DevTools
2. Убедитесь, что SameSite=lax
3. Проверьте SSL сертификаты

### Проблемы с перенаправлением
1. Проверьте CORS настройки
2. Убедитесь, что домены правильно настроены
3. Проверьте консоль браузера на ошибки

### Логирование
```typescript
// Включение отладочного режима
const DEBUG_CROSS_DOMAIN = process.env.NODE_ENV === 'development';

if (DEBUG_CROSS_DOMAIN) {
  console.log('Cross-domain operation:', { from, to, action });
}
```

## 📈 Мониторинг

### Метрики для отслеживания
- Количество успешных аутентификаций
- Количество перенаправлений
- Ошибки аутентификации
- Время загрузки страниц

### Алерты
- Ошибки CORS
- Проблемы с куки
- Сбои аутентификации

## 🔮 Будущие улучшения

- [ ] JWT токены вместо простых токенов
- [ ] Refresh токены для автоматического обновления
- [ ] Аналитика использования
- [ ] A/B тестирование потоков аутентификации
- [ ] Поддержка OAuth провайдеров 